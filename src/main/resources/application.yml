mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mymemodemo3
    username: root
    password:
server:
  port: 8080
  address: 0.0.0.0   # 允许局域网访问
  servlet:
    context-path: '/mymemo'
    session:
      cookie:
        domain:

shiro:
  loginUrl: /index/login